#pragma once

typedef  int DataType;     // 노드내의 데이터 영역의 형명 재지정 (이 부분만 바꾸면 형변환 쉽게 가능)

						   //-------------------------------------
						   //   int type 보조 함수
						   //-------------------------------------
void printInt(DataType *p);  // int 값을 출력하는 함수 

